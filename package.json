{
  "name": "pure-mitten-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2026-07-03",
    "deploy": "wrangler pages deploy public --project-name=pure-mitten-site"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
